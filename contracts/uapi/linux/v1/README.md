# Linux UAPI v1

Planned home of cdev, mmap, memory-registration, wait, and `/dev/metafluxctl`
worker-broker layouts. Structures follow Linux UAPI rules with fixed-width types,
explicit padding, compat tests, and a new command for incompatible semantics.
