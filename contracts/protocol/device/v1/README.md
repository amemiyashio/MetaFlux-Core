# Device Protocol v1

Planned home of generation-bound command, completion, timeline, and device
capability bytes shared by every transport. Records contain handles and offsets,
never process pointers or native-language layout.
