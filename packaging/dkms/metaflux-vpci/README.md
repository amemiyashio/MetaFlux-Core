# metaflux-vpci DKMS

Base milestone-0.1.1.0 guest PCI driver package skeleton for `metaflux_pci.ko`.
Experimental bare-metal vroot packaging must never depend on this artifact for
launcher or `baremetal-vpci` qualification; the reverse is also true for
lifecycle-core release rows.
