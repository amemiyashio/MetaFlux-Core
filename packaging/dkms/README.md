# DKMS Packaging

Planned generic external-module source package. It builds through the target
kernel's Kbuild tree, carries compile-probed compatibility shims, and does not
claim one module binary works across kernel configurations.
