interface:
  display_name: "GPU Virtualization with vfio-user"
  short_description: "Implement vfio-user guest transport behavior"
  default_prompt: "Use $gpu-virtualization-vfio-user skill to implement the requested vfio-user guest transport change, or provide analysis when that is the request; select checks for the affected behavior."
