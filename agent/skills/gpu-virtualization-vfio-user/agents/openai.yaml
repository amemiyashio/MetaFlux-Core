interface:
  display_name: "GPU Virtualization with vfio-user"
  short_description: "Implement vfio-user guest transport behavior"
  default_prompt: "Use $gpu-virtualization-vfio-user skill to locate the actual guest/server consumer, implement its missing transport and DMA lifetime behavior, and qualify that path; keep analysis-only requests read-only."
