interface:
  display_name: "CPU Backend Performance"
  short_description: "Implement and tune CPU execution and warm paths"
  default_prompt: "Use $cpu-backend-performance skill to implement the requested CPU execution or target-lowering change, or provide analysis when that is the request; select checks for the affected behavior."
