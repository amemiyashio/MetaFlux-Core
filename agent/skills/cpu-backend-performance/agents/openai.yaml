interface:
  display_name: "CPU Backend Performance"
  short_description: "Execute CPU kernel families and remove warm launch costs"
  default_prompt: "Use $cpu-backend-performance skill to trace the requested behavior to its actual CPU executor and implement the missing family or remove measured warm-path work when requested. For analysis, review or benchmarking, stay in that mode. Reuse the current profile and select evidence for the affected path."
