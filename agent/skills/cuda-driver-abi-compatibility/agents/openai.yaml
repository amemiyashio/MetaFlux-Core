interface:
  display_name: "CUDA Driver ABI Compatibility"
  short_description: "Implement CUDA Driver calls and object behavior"
  default_prompt: "Use $cuda-driver-abi-compatibility skill to implement the requested CUDA Driver ABI or object-lifecycle change, or provide analysis when that is the request; select checks for the affected behavior."
