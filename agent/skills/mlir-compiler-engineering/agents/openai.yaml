interface:
  display_name: "MLIR Compiler Engineering"
  short_description: "Implement Kernel IR conversion and pass pipelines"
  default_prompt: "Use $mlir-compiler-engineering skill to implement the requested Kernel IR conversion or pass-pipeline change, or provide analysis when that is the request; select checks for the affected behavior."
