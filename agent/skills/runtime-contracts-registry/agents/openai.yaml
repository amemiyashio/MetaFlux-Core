interface:
  display_name: "Runtime Contracts and Registry"
  short_description: "Trace neutral requests, views and shared lifetimes"
  default_prompt: "Use $runtime-contracts-registry skill to trace a neutral request or registry transition from its canonical fields to actual consumers, implement the lifetime change, and check its affected scenario; preserve requested analysis scope."
