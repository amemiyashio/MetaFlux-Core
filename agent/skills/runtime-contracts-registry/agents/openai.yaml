interface:
  display_name: "Runtime Contracts and Registry"
  short_description: "Implement neutral contracts and their consumers"
  default_prompt: "Use $runtime-contracts-registry skill to implement the requested neutral protocol, registry or shared-memory change, or provide analysis when that is the request; select checks for the affected behavior."
