interface:
  display_name: "Integrate MetaFlux Batch"
  short_description: "Automatically accept and verify one qualified Batch"
  default_prompt: "Use $integrate-batch to accept these qualified committed Iteration revisions."
policy:
  allow_implicit_invocation: true
