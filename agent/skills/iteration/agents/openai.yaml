interface:
  display_name: "MetaFlux Iteration"
  short_description: "Implement and verify one assigned product delivery"
  default_prompt: "Use $iteration skill to implement this assigned observable behavior, review its real execution boundaries, and deliver the qualified candidate."
policy:
  allow_implicit_invocation: true
