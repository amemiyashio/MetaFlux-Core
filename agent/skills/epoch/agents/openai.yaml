interface:
  display_name: "MetaFlux Epoch"
  short_description: "Audit and apply an explicitly confirmed Epoch cutover"
  default_prompt: "Use $epoch skill to audit this explicitly requested governance change, apply the confirmed cutover, and publish it after the required consolidated regression."
policy:
  allow_implicit_invocation: false
