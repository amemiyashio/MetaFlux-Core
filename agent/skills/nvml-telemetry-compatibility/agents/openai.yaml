interface:
  display_name: "NVML Telemetry Compatibility"
  short_description: "Trace NVML queries to real producers and snapshots"
  default_prompt: "Use $nvml-telemetry-compatibility skill to trace a requested nvidia-smi field or setter to its real producer and snapshot, implement its NVML mapping, and verify freshness and errors; preserve requested analysis scope."
