interface:
  display_name: "PTX SIMT Semantics"
  short_description: "Implement PTX forms and independent semantic oracles"
  default_prompt: "Use $ptx-simt-semantics skill to implement the requested PTX or Kernel IR semantic change, or provide analysis when that is the request; select checks for the affected behavior."
