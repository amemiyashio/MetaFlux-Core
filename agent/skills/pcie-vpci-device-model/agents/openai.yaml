interface:
  display_name: "PCIe and vPCI Device Model"
  short_description: "Review PCI config, BAR, MSI-X, and hotplug models"
  default_prompt: "Use $pcie-vpci-device-model to review this PCI configuration, BAR, MSI-X, enumeration, or hotplug change."
