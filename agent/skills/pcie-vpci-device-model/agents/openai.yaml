interface:
  display_name: "PCIe and vPCI Device Model"
  short_description: "Review PCI config, visibility, binding, and hotplug"
  default_prompt: "Use $pcie-vpci-device-model skill to review this PCI configuration, BAR, MSI-X, scan visibility, driver binding, or hotplug change."
