interface:
  display_name: "MetaFlux Batch"
  short_description: "Integrate and accept a verified slice or whole work item"
  default_prompt: "Use $batch skill to prepare the exact qualified delivery, obtain fresh integration evidence and publish its accepted state."
policy:
  allow_implicit_invocation: true
