interface:
  display_name: "MetaFlux Batch"
  short_description: "Integrate and accept a verified slice or whole work item"
  default_prompt: "Use $batch skill to accept this exact verified MetaFlux delivery and advance its route."
policy:
  allow_implicit_invocation: true
