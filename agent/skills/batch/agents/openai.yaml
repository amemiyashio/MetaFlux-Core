interface:
  display_name: "MetaFlux Batch"
  short_description: "Integrate and accept a verified slice or whole work item"
  default_prompt: "Use $batch skill to review this delivery's actual behavior, verify the merged scope, then accept and publish its exact revision."
policy:
  allow_implicit_invocation: true
