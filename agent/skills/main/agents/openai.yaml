interface:
  display_name: "MetaFlux Main"
  short_description: "Control repository work and resume verified transitions"
  default_prompt: "Use $main skill to manage this MetaFlux task through review, verification, and delivery."
policy:
  allow_implicit_invocation: true
