interface:
  display_name: "Linux Device Driver UAPI"
  short_description: "Implement Linux cdev, UAPI and buffer lifetimes"
  default_prompt: "Use $linux-device-driver-uapi skill to implement the requested Linux device UAPI or buffer-lifetime change, or provide analysis when that is the request; select checks for the affected behavior."
