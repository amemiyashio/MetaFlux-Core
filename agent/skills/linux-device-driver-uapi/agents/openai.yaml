interface:
  display_name: "Linux Device Driver UAPI"
  short_description: "Implement Linux cdev, UAPI and buffer lifetimes"
  default_prompt: "Use $linux-device-driver-uapi skill to trace the requested cdev operation through its fd/VMA/buffer owners, implement the handler and unwind, and select affected evidence; keep analysis-only requests read-only."
