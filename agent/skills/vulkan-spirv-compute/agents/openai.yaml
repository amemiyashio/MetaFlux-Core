interface:
  display_name: "Vulkan and SPIR-V Compute"
  short_description: "Implement Vulkan execution and resource lifetimes"
  default_prompt: "Use $vulkan-spirv-compute skill to implement the requested Vulkan execution, SPIR-V or resource-lifetime change, or provide analysis when that is the request; select checks for the affected behavior."
