interface:
  display_name: "Vulkan and SPIR-V Compute"
  short_description: "Review Vulkan compute, SPIR-V, cache, and device loss"
  default_prompt: "Use $vulkan-spirv-compute to review this Vulkan compute capability, SPIR-V, synchronization, cache, or device-loss change."
