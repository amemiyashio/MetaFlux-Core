---
id: P20260831-062
status: Recorded
captured: 2026-08-31
milestone: M0110
workstream: W0112
branch: main
git_revision: 6f047f4
workspace: generation-bound cdev launch resolution into CPU backend submit
---

# M0110 W0112 cdev Launch Checkpoint

## Outcome

The local cdev client now encodes a primary-entry launch descriptor with the
session generation plus module and argument-block object references. The C++
worker accepts an explicit generation-bound `CdevLaunchResolver`; daemon-owned
object semantics remain in the resolver, which returns a payload-relative,
already encoded backend argument block. The worker bounds-checks that block,
enforces primary-entry and 2D dimensions, constructs `mf_backend_launch_v1`,
and maps the synchronous backend result into the shared completion record.

The real CPU backend canonical Add fixture passes through this cdev worker path,
while fake-backend cases cover successful field translation, resolver stale
status, payload overflow, and malformed launch flags.

## Verification evidence

| Gate | Result |
|---|---|
| cdev client/worker focused tests | Passed: 2/2 |
| CPU backend and canonical Add focused tests | Passed: 2/2 |
| Full development CTest | Passed: 84/84 |
| Build and formatting | Passed: CMake build, clang-format, and `git diff --check` |
| Record validation before content commit | Passed: 57 sessions, 384 events, 328 Markdown files |
| Content identity | Passed: `6f047f4`, Agent Harness (codex) as Author and Committer |

## Boundary

This stage closes the synchronous cdev-to-CPU launch prerequisite, not W0112.
Registered-memory DMA mapping, backend in-flight references, generation
replacement, asynchronous events, daemon production resolver wiring, and
kernel/transport fault qualification remain open. The payload-relative argument
block is borrowed for one synchronous submit and is not an asynchronous
ownership contract.

## Cleanup

- Removed: temporary debugger probe and diagnostic output; no build tree,
  source snapshot, dependency store, download, or routine log was retained.
- Retained: cdev launch encoding, resolver seam, CPU Add regression, plan
  boundary, and this compact checkpoint.

## roast

### light roasts

- cdev primary-entry launch encoding -> `transports/cdev/client/src/cdev.c` (`6f047f4`; client CTest)
- generation-bound resolver contract -> `transports/cdev/worker/include/metaflux/transport/cdev_worker.hpp` (`6f047f4`; worker CTest)
- CPU Add through cdev submit -> `transports/cdev/worker/tests/worker_test.cpp` (`6f047f4`; full CTest)

### medium roasts

- W0112 launch boundary -> `agent/plan/M0110-kernel-guest-transport/work/W0112-local-cdev.md` (P062; DMA and replacement remain open)

### dark roasts

- none.

## session-only

- Host-local CPU execution output - reason: it demonstrates the current
  synchronous resolver path but is not physical NVIDIA or release qualification
  evidence.

## Handoff

Resume from `6f047f4` and P062. Read W0112 plus the registered-memory stage,
then bind a production resolver to generation-bound registered memory. Preserve
the backend-agnostic worker seam and add in-flight DMA reference accounting
before attempting daemon replacement or asynchronous completion.
