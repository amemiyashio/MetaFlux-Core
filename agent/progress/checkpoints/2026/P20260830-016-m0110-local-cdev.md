---
id: P20260830-016
status: Recorded
captured: 2026-08-30
milestone: M0110
workstream: W0112
branch: main
git_revision: c53b5b6
workspace: W0112 local cdev client/worker and kernel broker compile stage is implemented; ownership and fault qualification remain active
---

# M0110 W0112 local cdev stage

## Outcome

The first local cdev stage now has separate C17 client and C++20 worker halves,
paired submission/completion rings sourced from the generated transport schema,
and a Linux Kbuild module exposing `/dev/metafluxctl` and `/dev/metaflux0`. The
module negotiates the candidate UAPI, maps a generation-bound queue, and enforces
one worker lease per generation without changing the inherited M0100 descriptor.

## Verification evidence

| Gate | Result |
|---|---|
| Transport schema validator | 4 definitions and 12 records passed |
| Component graph | 15 components and 18 dependency edges passed |
| Focused CTest | schema C/C++/validator plus cdev client/worker, 5/5 passed |
| Target Kbuild | Linux 6.18.42 default GCC built `metaflux_core.ko`; modpost passed |
| LLVM Kbuild | Not qualified on current CachyOS flags; GCC-only options reject LLVM before source compile |
| Agent records | `python3 tools/check-agent-records.py .` passed |

## Boundary

W0112 remains Active. Eventfd attachment, registered-memory/pinning, daemon
generation replacement, backend payload wiring, KUnit, sanitizer, and owner/death
teardown evidence are still required before the workstream exit gate.

## Cleanup

- Removed: session-owned Kbuild objects, module output, and generated header.
- Retained: durable source, schema projection changes, and compact records in Git.
