---
id: P20260831-036
status: Recorded
captured: 2026-08-31
milestone: M0110
workstream: W0112
branch: main
git_revision: 70f332c
workspace: Offline cdev queue VMA tombstone backing now reaps on the final VMA close; queue krefs and generation replacement remain open
---

# M0110 W0112 Queue VMA Reaping

## Outcome

The cdev queue now has the same backing-lifetime rule as the payload arena.
Module teardown marks the queue offline and retains its mapping while VMAs are
still present. The final queue VMA close takes the cdev lock, decrements the
reference, and reclaims the mapping through one helper. The static queue object
and its generation remain a tombstone until daemon-controlled replacement is
implemented.

## Verification evidence

| Gate | Result |
|---|---|
| Target Kbuild | Passed: Linux 6.18.42 GCC built `metaflux_core.ko` and modpost completed |
| Full development CTest | Passed: 79/79 through `nix develop . --command ctest --preset dev` |
| Formatting and diff checks | Passed: `git diff --check` |
| Commit identity | `Agent Harness (codex)` as Author and Committer for content `70f332c` |

## Boundary

This checkpoint closes only the queue mapping backing-reap gap. It does not
claim queue krefs, backend reference draining, daemon-controlled generation
replacement, owner-death qualification, or the W0112 exit gate.

## Cleanup

- Removed: none; ignored Kbuild output remains under the external build owner.
- Retained: queue VMA reap helper, kernel documentation, and this checkpoint.

## roast

### light roasts

- Queue VMA tombstone backing reap -> `kernel/core/metaflux_core_main.c`
  (content `70f332c`; Linux 6.18.42 GCC Kbuild and full CTest 79/79)

### medium roasts

- none.

### dark roasts

- none.

## session-only

- The target kernel emitted a compiler minor-version mismatch warning; this is
  local build evidence and does not change the supported toolchain policy.

## Handoff

Resume S0112/W0112 with queue object references and generation replacement.
Keep the queue object as a static offline tombstone, and do not infer complete
daemon-death or backend-DMA safety from the VMA backing fix.
