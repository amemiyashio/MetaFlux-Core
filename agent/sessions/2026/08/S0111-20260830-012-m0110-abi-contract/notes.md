# Notes

The schema uses fixed-width little-endian records and explicit reserved bytes. The manifest currently imports the inherited descriptor, shared ring metadata, transport negotiation/message/completion/BAR records, and Linux cdev/worker UAPI records. W0114 must still freeze the candidate as transport v1 after both Add/Copy paths and fault evidence.
