# Notes

The selected design separates reusable behavior from transient inputs. The
workflow lives in a repository skill, while each guidance packet belongs to one
active session and is deleted after its disposition is distilled. The owning
agent remains responsible for source edits, verification, and durable
decisions; a specialist supplies direction or a candidate patch only.
