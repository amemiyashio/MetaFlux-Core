# Notes

- Scope is limited to the active M0100 implementation session and the generic
  session scaffolding/validation that reproduced its stale `ended_at` state.
- Git owns revision history. Session records retain durable outcomes, decisions,
  verification claims, failed-route lessons, unresolved work, and a resume point;
  they do not retain a second source or command transcript.
