# Kernel PCI Driver

Planned home of `metaflux_pci.ko`, the common driver for MetaFlux PCI functions.
It binds transport resources to the canonical cdev model and does not implement
NVIDIA RM/UVM behavior.
