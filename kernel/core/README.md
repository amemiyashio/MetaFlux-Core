# Kernel Core

Planned home of `metaflux_core.ko`: cdev objects, `/dev/metafluxctl` worker
broker, sessions, queues, mappings, waits, reference-safe teardown, and
generation tombstones. Backend policy and compute semantics stay in userspace.
